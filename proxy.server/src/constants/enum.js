export const ERole = Object.freeze({
  USER: 'user',
  ADMIN: 'admin'
})

export const ECategoryPackage = Object.freeze({
  PAIDPACKAGE: 'PaidPackage',
  PROFILEPACKAGE: 'ProfilePackage'
})

export const EPaymentMethods = Object.freeze({
  EWALLET: 'EWallet',
  CASH: 'Cash',
  CREDIT: 'Credit'
})

export const EOrderStatus = Object.freeze({
  PENDING: 'Pending',
  CONFIRMED: 'Confirmed',
  CANCELED: 'Canceled'
})
